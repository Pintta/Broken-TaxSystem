# Broken-TaxSystem
FiveM QBCore tax system script, ready to go!
```lua
--------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------
-- The system calculates the tax amount and automatically adds it to the database.  --
-- TriggerServerEvent('Broken-TaxSystem:addTax', Player.PlayerData.citizenid, PayTotal) --
--------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------

TriggerServerEvent('Broken-TaxSystem:addTax', Player.PlayerData.citizenid, PayTotal)
```
